version: 2.1

jobs:
  build-and-test:
    machine:
      image: ubuntu-2004:current
    steps:
      - checkout
      - run: make deps
      - run: make test

workflows:
  build-and-test-workflow:
    jobs:
      - build-and-test